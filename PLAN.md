# Domain Graph 開発計画

## 目的

議事録、要件、設計、課題、GitHub の開発情報を、出所付きの知識グラフとして Neo4j に集約する。利用者と AI エージェントが「何が、どの情報から発生し、何に影響するか」を追跡できる状態を作り、設計・実装のズレを早期に発見する。

## MVP の到達点

次の一連の流れを、ローカル環境で再現できることを最初の完成条件とする。

```text
Markdown / JSON を inputs に配置
  → 入力を検証・正規化
  → Neo4j に冪等取り込み
  → 出所付きの関係を Cypher/API で検索
  → 「要件から実装まで」の根拠付き結果を取得
```

初期 MVP では対象を Markdown/JSON の議事録・要件・設計情報に絞る。GitHub、スプレッドシート、MCP、CI/CD、クラウド運用は MVP のグラフモデルと入力契約が固まった後に追加する。

## フェーズ

### Phase 0: 方針・開発基盤

- MVP の利用シナリオと受け入れ条件を固定する
- TypeScript/Node.js、テスト、lint、CI の最小構成を決める
- Neo4j を Docker Compose で起動できるようにする

### Phase 1: 最小知識グラフ

- ノード、関係、識別子、出所、承認状態のスキーマを定義する
- 入力フォーマットと `inputs/` の規約を定義する
- Markdown/JSON を正規化し、Neo4j に冪等取り込みする
- 代表的な Cypher クエリと問い合わせ API を提供する

### Phase 2: 開発情報との接続

- GitHub Issue/PR/commit を出所付きで取り込む
- 課題管理表・スプレッドシートの CSV/JSON インポートを追加する
- 差分取り込み、重複排除、失敗時の再実行を整備する

### Phase 3: エージェント・品質検証

- MCP Server で関係探索、出所確認、影響範囲確認を提供する
- Codex、Copilot、Claude Code などから使える認証・権限制御を設計する
- PR 時に未対応要件、孤立設計、出所不足、関係矛盾を検出する

### Phase 4: チーム運用

- コンテナまたは EC2 上の閉じた環境へデプロイする
- 短期トークン、最小権限、監査ログ、プロジェクト分離を実装する
- バックアップ、復旧、監視、データ保持期間を定義する

## 優先順位

優先度は「正しい関係を、根拠付きで、再現可能に検索できること」を最上位とする。

1. MVP の利用シナリオと受け入れ条件
2. Neo4j 開発環境とグラフスキーマ
3. 入力契約・正規化・冪等取り込み
4. 根拠付き問い合わせ API
5. GitHub/課題管理表の連携
6. MCP と AI エージェント連携
7. PR 品質検証
8. クラウド運用・セキュリティ強化

## 共通の受け入れ条件

- 取り込んだノードと関係から、元の入力・URL・コミットなどの出所を辿れる
- 同じ入力を複数回処理しても、重複ノードや重複関係が増えない
- 不明な関係や AI の推論結果は、事実と区別して保存される
- 根拠のない回答は「未登録」または「根拠不足」と返せる
- 秘密情報がログ、テスト用データ、グラフ属性に混入しない
- 主要な処理に自動テストと再現可能なサンプルデータがある

## Issue の運用

Issue はこの計画の作業単位とし、依存関係を本文に記載する。実装 Issue では、目的、対象外、入力/出力、出所、受け入れ条件、テスト方法を明記する。設計上の判断は Issue または `docs/decisions/` に記録し、後からグラフへ取り込める形にする。
