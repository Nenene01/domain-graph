# AGENTS.md

## プロジェクト概要

このリポジトリは、業務・ドメイン・要件・機能・設計・実装・課題・意思決定の関係と出所を Neo4j に蓄積し、設計と実装の不整合を発見するための Domain Graph プロダクトを開発します。詳細なコンセプトは [README.md](README.md) を参照してください。

## 開発時の原則

- 変更の目的、根拠、影響範囲を追跡可能にする。
- 外部情報を取り込む処理では、必ず出所、取得日時、識別子、更新元を保持する。
- 自動抽出データと人間が承認した定義を区別する。
- Neo4j への取り込みは冪等にし、再実行しても重複ノード・重複関係を作らない。
- AI による推論結果は事実と断定せず、信頼度と根拠を付ける。
- 秘密情報、アクセストークン、個人情報をリポジトリ、グラフ、ログへ不用意に保存しない。
- エージェント向け API/MCP は最小権限、短期トークン、監査可能性を前提にする。
- 自動でコードや設計を変更する機能は、明示的な承認フローを経て実行する。

## 入力情報の扱い

入力は `inputs/` 配下に情報源ごとのディレクトリを設け、機械処理可能な形式とメタデータを添えて保存します。元データを上書きせず、正規化後のデータとの対応関係を保持してください。

想定する情報源は、議事録、課題管理表、GitHub Issue/PR、要件、設計書、ソースコードのメタデータです。

## 実装前に確認すること

1. その変更がどの業務・要件・設計上の問題を解決するか
2. どのノード、関係、入力形式、API に影響するか
3. 出所と更新履歴をどう保持するか
4. 冪等性、権限、秘密情報、監査ログに問題がないか
5. 再現可能なテストや検証クエリを追加できるか

## 設計上の用語

初期の代表的な関係は `DEFINED_BY`、`RELATES_TO`、`IMPLEMENTS`、`VERIFIES`、`BLOCKS`、`CHANGED_BY`、`DECIDED_IN`、`DERIVED_FROM` とします。実装で追加・変更する場合は、意味、向き、必須属性、出所を記録してください。

## エージェントの回答方針

- 回答は、結論だけでなく根拠となるノード・関係・原文への参照を示す。
- グラフに存在しない情報は推測で補完せず、「未登録」「根拠不足」と明示する。
- 矛盾する定義がある場合は、情報源、更新日時、承認状態を並べて判断を保留する。
- 設計・実装ミスの指摘では、該当箇所、期待される定義、影響範囲、確認方法を分けて示す。
